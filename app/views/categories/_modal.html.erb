<div class="modal fade" id="categoryModal" tabindex="-1" aria-labelledby="categoryModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="categoryModalLabel">Confirmation</h5>
      </div>
      <div class="modal-body">
        This will delete the current category and its associated tasks. Proceed?
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
        <%= link_to category_path(current_user.username, @category), data: { turbo_method: :delete }, class: "btn btn-primary" do %>
          Continue
        <% end %>
      </div>
    </div>
  </div>
</div>