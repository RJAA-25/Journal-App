<%= render "layouts/components/breadcrumb", profile: false, category: @category ||= nil, task: @task ||= nil %>

<div class="container my-4">
  <div class="container p-3 bg-white shadow">
    <div class="d-flex justify-content-between align-items-center">
      <h1>Create Category</h1>
      <%= link_to "Cancel", dashboard_path(current_user.username), class: "btn btn-outline-primary btn-sm" %>
    </div>
    <%= render "form_create" %>
  </div>
</div>

